<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springboot.mapper.RoleMapper">

    <!-- 显式别名，保证任何配置下都能映射到实体字段 -->
    <sql id="roleColumns">
        role_id     AS roleId,
        role_name   AS roleName,
        role_key    AS roleKey,
        role_sort   AS roleSort,
        data_scope  AS dataScope,
        status,
        del_flag    AS delFlag,
        create_by   AS createBy,
        create_time AS createTime,
        update_by   AS updateBy,
        update_time AS updateTime,
        remark
    </sql>

    <!-- 条件分页 -->
    <select id="page" resultType="com.example.springboot.entity.Role">
        SELECT
        <include refid="roleColumns"/>
        FROM sys_role
        <where>
            del_flag = '0'
            <if test="roleName != null and roleName != ''">
                AND role_name LIKE CONCAT('%', #{roleName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY role_sort
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM sys_role
        <where>
            del_flag = '0'
            <if test="roleName != null and roleName != ''">
                AND role_name LIKE CONCAT('%', #{roleName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 全部启用角色（下拉框） -->
    <select id="listAll" resultType="com.example.springboot.entity.Role">
        SELECT
        <include refid="roleColumns"/>
        FROM sys_role
        WHERE del_flag = '0'
          AND status = '0'
        ORDER BY role_sort
    </select>

    <select id="selectByRoleId" resultType="com.example.springboot.entity.Role">
        SELECT
        <include refid="roleColumns"/>
        FROM sys_role
        WHERE role_id = #{roleId}
          AND del_flag = '0'
    </select>

    <!-- 按 role_key 查角色 -->
    <select id="selectByRoleKey" resultType="com.example.springboot.entity.Role">
        SELECT
        <include refid="roleColumns"/>
        FROM sys_role
        WHERE role_key = #{roleKey}
          AND del_flag = '0'
        LIMIT 1
    </select>

    <!-- 新增角色 -->
    <insert id="insert" parameterType="com.example.springboot.entity.Role"
            useGeneratedKeys="true" keyProperty="roleId">
        INSERT INTO sys_role (
            role_name, role_key, role_sort, data_scope, status,
            del_flag, create_by, create_time, remark
        )
        VALUES (
            #{roleName}, #{roleKey}, #{roleSort}, #{dataScope}, #{status},
            '0', #{createBy}, NOW(), #{remark}
        )
    </insert>

    <update id="update">
        UPDATE sys_role
        SET role_name   = #{roleName},
            role_key    = #{roleKey},
            role_sort   = #{roleSort},
            data_scope  = #{dataScope},
            status      = #{status},
            remark      = #{remark},
            update_time = NOW()
        WHERE role_id = #{roleId}
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteByRoleId">
        UPDATE sys_role
        SET del_flag = '1',
            update_time = NOW()
        WHERE role_id = #{roleId}
    </update>

    <!-- ==================== 用户-角色 sys_user_role ==================== -->

    <delete id="deleteUserRoleByRoleId">
        DELETE FROM sys_user_role WHERE role_id = #{roleId}
    </delete>

    <select id="selectRoleIdsByUserId" resultType="long">
        SELECT role_id FROM sys_user_role WHERE user_id = #{userId}
    </select>

    <!-- 某用户的角色列表 -->
    <select id="selectRolesByUserId" resultType="com.example.springboot.entity.Role">
        SELECT
            r.role_id     AS roleId,
            r.role_name   AS roleName,
            r.role_key    AS roleKey,
            r.role_sort   AS roleSort,
            r.data_scope  AS dataScope,
            r.status,
            r.del_flag    AS delFlag,
            r.create_by   AS createBy,
            r.create_time AS createTime,
            r.update_by   AS updateBy,
            r.update_time AS updateTime,
            r.remark
        FROM sys_role r
        INNER JOIN sys_user_role ur ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId}
          AND r.del_flag = '0'
        ORDER BY r.role_sort
    </select>

    <delete id="deleteUserRoleByUserId">
        DELETE FROM sys_user_role WHERE user_id = #{userId}
    </delete>

    <insert id="insertUserRole">
        INSERT INTO sys_user_role (user_id, role_id)
        VALUES
        <foreach collection="roleIds" item="rid" separator=",">
            (#{userId}, #{rid})
        </foreach>
    </insert>

    <!-- ==================== 角色-菜单 sys_role_menu ==================== -->

    <select id="selectMenuIdsByRoleId" resultType="long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
    </select>

    <delete id="deleteRoleMenuByRoleId">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRoleMenu">
        INSERT INTO sys_role_menu (role_id, menu_id)
        VALUES
        <foreach collection="menuIds" item="mid" separator=",">
            (#{roleId}, #{mid})
        </foreach>
    </insert>

    <!-- ==================== 角色-部门 sys_role_dept ==================== -->

    <select id="selectDeptIdsByRoleId" resultType="long">
        SELECT dept_id FROM sys_role_dept WHERE role_id = #{roleId}
    </select>

    <delete id="deleteRoleDeptByRoleId">
        DELETE FROM sys_role_dept WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRoleDept">
        INSERT INTO sys_role_dept (role_id, dept_id)
        VALUES
        <foreach collection="deptIds" item="did" separator=",">
            (#{roleId}, #{did})
        </foreach>
    </insert>

    <!-- ==================== 权限标识 ==================== -->

    <!-- 用户全部菜单权限标识：sys_user_role -> sys_role_menu -> sys_menu.perms -->
    <select id="selectPermsByUserId" resultType="string">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON rm.menu_id = m.menu_id
        INNER JOIN sys_user_role ur ON ur.role_id = rm.role_id
        WHERE ur.user_id = #{userId}
          AND m.status = '0'
          AND m.perms IS NOT NULL
          AND m.perms != ''
    </select>

</mapper>
